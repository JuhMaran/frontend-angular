/**
 * Equivalente ao enum ContainerCategory do backend.
 * Representa a categoria física do recipiente (copo, garrafa, barril etc.).
 */
export enum ContainerCategory {
    GLASS = 'GLASS',        // Copo de Vidro
    BOTTLE = 'BOTTLE',      // Garrafa
    GROWLER = 'GROWLER',    // 
    KEG = 'KEG'             // Barril
}
